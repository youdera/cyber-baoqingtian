# 项目协作约定

每次开始或恢复工作、执行新一轮采集前，读取HANDOFF.md并检查最新日志；结束后追加日志，不覆盖历史记录。

- 当前0.2为本地公开公告浏览工具，入口notice_app.py；start.cmd及app.py命令均进入新版。旧版源码与数据库为历史兼容材料，不是当前产品接口。
- 公示发布时间与招考年度独立，至少设置一种；只填一端按该月或该年查询，两种都填时取交集。公示月份包含首尾日。未知地区、日期、类型及失败如实显示。
- 仅保存公告元数据与官方原文链接，不下载或索引人员名单，不做姓名、学校、籍贯、电话的身份关联，不自动发送举报。
- 不将旧数据自动迁入公告库，不批量删除用户历史文件。data/和handoff的incoming/outgoing不得提交Git。
- 不绕过验证码、认证或访问限制，不关闭TLS验证；来源固定在代码中。
- 不以站点数、页数或日期跨度宣称全国或历史完整覆盖。partial保留具体原因。
- 当前能力以docs/RELEASE_0_2.md为准。变更后运行对应脱敏测试与必要的浏览器验收。
